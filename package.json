{
  "name": "appica-country-flags",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "codegen": "pnpm -r codegen",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run && pnpm -r test",
    "clean:generated": "pnpm -r clean:generated",
    "clean:dist": "pnpm -r clean:dist",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "pack:react": "pnpm --filter @appica/country-flags-react run release:check && pnpm --filter @appica/country-flags-react exec pnpm pack --config.ignore-scripts=true",
    "pack:svelte": "pnpm --filter @appica/country-flags-svelte run release:check && pnpm --filter @appica/country-flags-svelte exec pnpm pack --config.ignore-scripts=true",
    "release": "pnpm --filter @appica/country-flags-react publish"
  },
  "devDependencies": {
    "prettier": "^3.9.9",
    "prettier-plugin-svelte": "^3.4.0",
    "vitest": "^5.0.1"
  }
}
